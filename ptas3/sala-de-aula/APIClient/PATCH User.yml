info:
  name: PATCH User
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base}}/users/1"
  body:
    type: json
    data: |-
      {
          "id": 1,
          "nome": "Ana Júlia",
          "idade": 15,
          "email": "anajulia@email.com"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
