info:
  name: PATCH User
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/users/8"
  body:
    type: json
    data: |-
      {
        "deletedAt": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
