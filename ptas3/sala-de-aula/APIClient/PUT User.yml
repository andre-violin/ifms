info:
  name: PUT User
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}/users/7"
  body:
    type: json
    data: |-
      {
        "email": "jurema@email.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
