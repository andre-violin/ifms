info:
  name: PUT User
  type: http

http:
  method: PUT
  url: "{{base}}/users/1"
  body:
    type: json
    data: |-
      {
          "id": 1,
          "nome": "Ana Júlia",
          "idade": 15,
          "email": "ana_julia@email.com"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
